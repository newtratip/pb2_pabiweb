<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="pb.repo.admin.dao.MainEmployeeLevelDAO">
    <resultMap id="result" type="mainEmployeeLevelModel">
        <result property="org" column="org"/>
        <result property="module" column="module"/>
        <result property="level" column="level"/>
        <result property="fromAmount" column="from_amount"/>
        <result property="toAmount" column="to_amount"/>
        <result property="totalRowCount" column="totalRowCount"/>
        
    </resultMap>
    
    <insert
		id="add"
		parameterType="mainEmployeeLevelModel"
		flushCache="true"
		statementType="PREPARED"
		keyProperty="user"
		useGeneratedKeys="true"
		>
	    insert into pb2_ext_employee_level(org,module,level,from_amount,to_amount)
    	values (#{org},#{module},#{level},#{fromAmount},#{toAmount})
		
	</insert>
	
	<select id="count"  resultType="long">
        select
		    count(1)
		from
		    pb2_ext_employee_level
	
    </select>
	
	<delete id="delete"
    		parameterType="mainEmployeeLevelModel"
			flushCache="true"
			statementType="PREPARED"
			>
   		delete from pb2_ext_employee_level where module=#{module} and org=#{org} and level=#{level} 
	</delete>
	
</mapper>
