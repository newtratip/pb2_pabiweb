<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="pb.repo.admin.dao.MainAccountActivityDAO">
    <resultMap id="result" type="mainAccountActivityModel">
        <result property="id" column="id"/>
        <result property="name" column="name"/>
        <result property="totalRowCount" column="totalRowCount"/>
        
    </resultMap>
    
    <select id="list" resultType="hashmap" parameterType="map">
        SELECT * FROM
	    (SELECT a.*,
			coalesce((select value
		    from pb2_ext_ir_translation
		    where name = 'account_activity,name'
		    and type = 'model' and lang='th_TH'
		    and res_id = a.id), name) as name_th  
		FROM   pb2_ext_account_activity a
		) b
	   <if test="terms != null">
			<foreach item="item" index="index" collection="terms" open="WHERE (" separator=" AND " close=")">
	    			concat(name,' ',name_th) ILIKE '%'||#{item}||'%'
	   		</foreach>
   		</if>  
	   ORDER BY ${orderBy}
    </select>
    
    <select id="get" resultType="map" parameterType="int">
        select * from pb2_ext_account_activity
        where id=#{id}
    </select>    
    	
</mapper>
