<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="pb.repo.admin.dao.MainEmployeeDAO">
    <resultMap id="result" type="mainEmployeeModel">
        <result property="id" column="emp_id"/>
        <result property="defaultCostCenter" column="default_cost_center"/>
        <result property="assignCostCenterIds" column="assign_cost_center_ids"/>
        <result property="totalRowCount" column="totalRowCount"/>
        
    </resultMap>
    
    <insert
		id="add"
		parameterType="mainEmployeeModel"
		flushCache="true"
		statementType="PREPARED"
		keyProperty="user"
		useGeneratedKeys="true"
		>
	    insert into pb2_ext_employee(emp_id, default_cost_center, assign_cost_center_ids)
    	values (#{id},#{defaultCostCenter},#{assignCostCenterIds})
		
	</insert>
	
	<select id="count"  resultType="long">
        select
		    count(1)
		from
		    pb2_ext_employee
	
    </select>
	
	<delete id="delete"
    		parameterType="string"
			flushCache="true"
			statementType="PREPARED"
			>
   		delete from pb2_ext_employee where emp_id=#{id}
	</delete>
	
</mapper>
